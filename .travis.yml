language: node_js
node_js:
  - "node"

before_install:
  - npm install

install:
  - npm install newman@3.9.3

before_script:
  - node --version
  - npm --version
  - node_modules/.bin/newman --version

script:
  - npm test
  - console.log('TESTS API');
  - node_modules/.bin/newman run test-tp2.postman_collection.json

